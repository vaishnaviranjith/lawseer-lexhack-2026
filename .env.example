# LAWSEER runs in deterministic demo mode with no key.
LAWSEER_REASONING_PROVIDER=demo

# Optional future server-side provider seam. Never put this in frontend code.
# LAWSEER_REASONING_PROVIDER=openai
# LAWSEER_LLM_API_KEY=replace-me
